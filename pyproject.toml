[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=6.0.0",
    "graphviz>=0.21",
    "pypdf>=6.14.2",
]
